# 📡 AUTORADAR — Агрегатор автозапчастей (Астана)

**AUTORADAR** — это высокотехнологичная платформа для поиска автозапчастей, работающая по модели "Биржа заявок". Клиент оставляет запрос, а продавцы конкурируют за него в режиме реального времени через Telegram.

## 🚀 Реализованный функционал

### 🛡 Безопасность и Инфраструктура
- **Rate Limiting:** Защита от спама на уровне Server Actions (лимит: 3 заявки / 15 мин с одного IP).
- **Webhook Security:** Валидация входящих запросов от Telegram через секретный токен `X-Telegram-Bot-Api-Secret-Token`.
- **KZ Phone Validation:** Строгая проверка казахстанских номеров телефонов с использованием Regex.
- **Type Safety:** Полная типизация взаимодействия с Supabase (Zero `any` policy).

### 🛠 Бизнес-логика (Анонимный аукцион)
- **Скрытие контактов:** Продавцы получают заявки без номера телефона клиента для предотвращения сделок "в обход".
- **Интерактивные кнопки:** Внедрена система Inline-кнопок в Telegram для моментального отклика (Предложить цену / Нет в наличии).
- **HUD Search:** Двухэтапная форма поиска (Деталь -> Контакты) с умным автокомплитом.

## 🛠 Технологический стек
- **Frontend:** Next.js 14 (App Router), TypeScript, Tailwind CSS, Shadcn UI.
- **Backend:** Next.js Server Actions, API Routes (Webhooks).
- **Database:** Supabase (PostgreSQL).
- **Bot Engine:** Telegraf.js.
- **UI Effects:** Framer Motion, CSS Radar Animations.

## 📂 Структура проекта
- `/app/search/actions.ts` — Ядро бизнес-логики и рассылки.
- `/app/api/bot/route.ts` — Обработчик команд и callback-запросов бота.
- `/lib/telegram.ts` — Утилита для работы с Telegram Bot API.
- `/utils/supabase/` — Конфигурация клиента БД (Server/Browser).